#FUNCIONES CON ARRAYS

import numpy as np
array = np.arange(5)
np.sqrt(array) # hace la raiz cuadrada de cada elemtno de la array
np.random.rand(5) # pone numeros random en una array

lista = [5,6,7,8,9]
array2 = np.array(lista)

np.add(array,array2) # suma dos arrays.
np.maximum(array,array2) #coge el maximo valor de las dos arrays.

